#include <stdio.h>
/**
 * mod - reponsible for finding reaminder two intergers
 * @k: represents first integer
 * @j: represtents second integer
 * Return: remainder result
 */
int mod(int k, int j)
{
	return (k % j);
}
