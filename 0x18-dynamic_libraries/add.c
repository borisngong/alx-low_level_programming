#include <stdio.h>
/**
 * add - reponsible for adding two intergers
 * @k: represents first integer
 * @j: represtents second integer
 * Return: return the sum
 */
int add(int k, int j)
{
	return (k + j);
}
