#include <stdio.h>
/**
 * sub - reponsible for the difference of two intergers
 * @k: represents first integer
 * @j: represtents second integer
 * Return: subtraction result
 */
int sub(int k, int j)
{
	return (k - j);
}
