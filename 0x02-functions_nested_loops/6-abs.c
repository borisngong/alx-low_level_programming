#include "main.h"
/**
  *_abs - computes absolute value of an interger
  *@j: input number of an interger
  *Return: Absolute number
  */
int _abs(int j)
{
	if (j > 0)
	{
		return (j);
	}
	else
	{
		return (j = -1);
	}
	_putchar('\n');
}
