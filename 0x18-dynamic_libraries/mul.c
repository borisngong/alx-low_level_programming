#include <stdio.h>
/**
 * mul - reponsible for multiplying two intergers
 * @k: represents first integer
 * @j: represtents second integer
 * Return: multiplication result
 */
int mul(int k, int j)
{
	return (k * j);
}
