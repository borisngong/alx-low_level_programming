#include "main.h"

/**
 * _islower - c function that hecks for lowercase character
 * @c: character to check
 * Return: 1 if c is lowercase and 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

