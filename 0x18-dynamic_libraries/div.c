#include <stdio.h>
/**
 * div - reponsible for dividing two intergers
 * @k: represents first integer
 * @j: represtents second integer
 * Return: division result
 */
int div(int k, int j)
{
	return (k / j);
}